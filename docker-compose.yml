version: "3.3"
services:
    pinx-vue-kit:
        build:
            context: .
        container_name: pinx-vue-kit
        restart: unless-stopped
        ports:
            - 3000:3000
        environment:
            NODE_ENV: production
